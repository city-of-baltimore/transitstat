name: Transitstat post commit steps
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
      - name: Checkout ridesystems repo
        uses: actions/checkout@v2
        with:
          repository: city-of-baltimore/ridesystems
          path: ridesystems
      - name: Install dependencies
        run: |
          pip install --upgrade pip wheel pylint flake8 bandit mypy wheel ./ridesystems
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: Lint with pylint
        run: |
          pylint python
      - name: Lint with flake8
        run : |
          flake8 --config=.flake8 python
      - name: Look for security issues with Bandit
        run: |
          bandit python
      - name: Type checking
        run: |
          mypy python
